NAME

  **ps** - output current process stack


SYNOPSIS

    ps

DESCRIPTION

    The ps command outputs the current process stack. Each process in
    the stack is listed on a new line, and both the process id and 
    process name are given. Note that in most cases, the first two processes 
    are init and dssh, and the final process is ps.

OPTIONS

  -h, --help:  output usage information and exit

SEE ALSO

    [env](/data/man/env.dsmd), [pwd](/data/man/pwd.dsmd)